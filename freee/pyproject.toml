[project]
name = "freee-mcp"
version = "0.1.0"
description = "MCP server for freee会計 API"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
freee-mcp = "freee_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
sources = {"src" = "freee_mcp"}
